import styled from 'styled-components';

const ServicesDashboard = styled.div`
  padding: 15px;
`;

const Header = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
`;

export const S = {
  ServicesDashboard,
  Header,
};
